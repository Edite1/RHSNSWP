<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>School Announcements</title>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="title">School Announcements</h1>
            <button class="admin-button" onclick="showAdminLogin()">Admin Login</button>
        </div>

        <div class="announcements" id="announcements">
            <div class="announcement">
                <div class="date-badge">OCT 29</div>
                <div class="content">
                    <span class="breaking">BREAKING</span>
                    <strong>Early Voting Week for Student Council Elections</strong>
                    <div class="meta">Voting stations open in the main hall from 12-2pm</div>
                    <img src="/api/placeholder/400/300" class="announcement-image" alt="Early Voting">
                </div>
            </div>
            <div class="announcement">
                <div class="date-badge">OCT 30</div>
                <div class="content">
                    <strong>Fall Sports Awards Ceremony</strong>
                    <div class="meta">Join us in the gymnasium at 7pm to celebrate our athletes</div>
                </div>
            </div>
        </div>
    </div>

    <!-- Admin Login Modal -->
    <div id="adminLoginModal" class="modal">
        <div class="modal-content">
            <h2>Admin Login</h2>
            <div class="form-group">
                <label>Password</label>
                <input type="password" id="adminPassword">
            </div>
            <div class="form-actions">
                <button class="close-button" onclick="closeModal()">Cancel</button>
                <button class="submit-button" onclick="verifyAdmin()">Login</button>
            </div>
        </div>
    </div>

    <script>
        const adminPassword = 'IBDGIC'; // Set the admin password

        function showAdminLogin() {
            document.getElementById('adminLoginModal').style.display = 'block';
        }

        function closeModal() {
            document.getElementById('adminLoginModal').style.display = 'none';
        }

        function verifyAdmin() {
            const inputPassword = document.getElementById('adminPassword').value;
            if (inputPassword === adminPassword) {
                alert('Login successful!');
                closeModal();
                // Here you could redirect to the admin panel or show further options
            } else {
                alert('Incorrect password');
            }
        }
    </script>
</body>
</html>
